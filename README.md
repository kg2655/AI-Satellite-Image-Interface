


  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.






  for backend open command prompt
  dir

  cd backend
venv\Scripts\activate



python -m uvicorn main:app
  